#!/usr/bin/env php
<?php

use Cyrus\ClassStat\Command\ClassAuthorStat;
use Cyrus\ClassStat\Command\ClassStatInfo;
use Symfony\Component\Console\Application;

require_once __DIR__ . '/../vendor/autoload.php';

//$application = new Application('PHP Static Analyzer', '1.0.0');
//
//$application->add(new ClassAuthorStat());
//
//$exitCode = $application->run();
//
//exit($exitCode);

$application = new Application('PHP Static Analyzer', '1.0.0');

$application->add(new ClassStatInfo());

$exitCode = $application->run();

exit($exitCode);